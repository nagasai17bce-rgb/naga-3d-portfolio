import {useEffect,useState} from 'react'; import Scene from './components/Scene'; import {experience,profile,projects,skills} from './data/content';
export default function App(){const [active,setActive]=useState(''); useEffect(()=>{const on=()=>{const y=window.scrollY; let id='hero'; if(y>window.innerHeight*.65)id='experience'; if(y>window.innerHeight*1.7)id='work'; if(y>window.innerHeight*2.7)id='contact'; setActive(id)}; addEventListener('scroll',on); on(); return()=>removeEventListener('scroll',on)},[]); return <><Scene/><nav><b>NS<span>.</span></b><div>{['hero','experience','work','contact'].map(x=><a className={active===x?'on':''} href={'#'+x} key={x}>{x}</a>)}</div></nav><main><section id="hero" className="hero"><div className="eyebrow">MTS · AI / BACKEND · INDIA</div><h1>{profile.name}<br/><em>builds useful intelligence.</em></h1><p>{profile.intro}</p><div className="actions"><a href="#work">Explore work ↓</a><a className="ghost" href="#contact">Let's build →</a></div><div className="scroll">SCROLL TO EXPLORE</div></section><section id="experience" className="section"><div className="label">01 / EXPERIENCE</div><h2>Systems that survive<br/><em>production.</em></h2><div className="timeline">{experience.map((e,i)=><article key={e.company}><span>{String(i+1).padStart(2,'0')}</span><div><small>{e.year}</small><h3>{e.company}</h3><strong>{e.role}</strong><p>{e.text}</p></div></article>)}</div></section><section id="work" className="section work"><div className="label">02 / SELECTED WORK</div><h2>Ideas → <em>working systems.</em></h2><div className="grid">{projects.map((p,i)=><article className="card" key={p.title}><div className="num">0{i+1}</div><small>{p.tag}</small><h3>{p.title}</h3><p>{p.text}</p><span className="arrow">↗</span></article>)}</div><div className="skills"><span>STACK</span>{skills.map(s=><b key={s}>{s}</b>)}</div></section><section id="contact" className="contact"><div className="label">03 / CONTACT</div><h2>Have a hard problem?<br/><em>Let's make it simple.</em></h2><p>Open to AI engineering, backend systems, product engineering and technology partnerships.</p><div className="actions"><a href="mailto:">Start a conversation →</a></div><footer>© {new Date().getFullYear()} {profile.name} · Built with React Three Fiber</footer></section></main></>}
